# from app.utils.ai_models import initialize_models

# print("Starting model initialization...")
# classifier, tokenizer, model, labels = initialize_models()
# print(f"Models initialized successfully with labels: {labels}")